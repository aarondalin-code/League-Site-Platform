# Derived data folder

This folder is for generated files that come from the source CSV files in `data/csv/`.

## Planned use

Later, this folder can hold:

- combined data files
- transformed data
- generated JSON files
